// il testo che segue è un programma di faust
// faust è un linguggio di programmazione funzionale  
// dsp = digital signal processing
// un commento è uma riga di codice ignorata dal compilatore
// ogni riga di faust temina con un carattere ;

import("stdfaust.lib"); // importare la libreria standart di faust

// gli operatori matematici sono + - * /
//crea applicazioni peer il trattamento digitale del segnale
//faust esegue il contenuto di process

process = +; //  in ogni programma ci può essere un solo process =
//il testo che segue è un programma di faust
